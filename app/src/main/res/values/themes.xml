<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GlyphAODToys" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GlyphAODToys" parent="Base.Theme.GlyphAODToys" />
</resources>